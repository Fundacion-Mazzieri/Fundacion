@model Fundacion.Models.Role

@{
    ViewData["Title"] = "Details";
}

<h1>Detalles de Roles:</h1>

<div>
    <h4>Roles</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.RoDenominacion)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.RoDenominacion)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.RoId" class="btn btn-warning">Modificar</a> 
    <a asp-action="Index" class="btn btn-primary">Volver</a>
</div>
